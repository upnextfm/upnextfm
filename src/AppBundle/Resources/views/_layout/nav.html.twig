<nav>
    <div class="nav-wrapper up-nav-wrapper">
        <a href="/" class="brand-logo">
            <img src="{{ asset('images/logo-brand.png') }}" />
        </a>
        <a href="#" id="up-nav-toggle" data-activates="slide-out" class="button-collapse">
            <i class="material-icons">menu</i>
        </a>
        <ul id="nav-mobile" class="right">
            <li class="hide-on-med-and-down">
                <a href="{{ path("playlist_recent") }}">Recent</a>
            </li>
            <li class="hide-on-med-and-down">
                {% if app.user %}
                    <a href="{{ path("fos_user_security_logout") }}">Logout</a>
                {% elseif not app.user %}
                    <a href="{{ path("fos_user_security_login") }}">Login</a>
                {% endif %}
            </li>
            {% if app.user %}
                <li>
                    <a href="{{ path("profile", {username: app.user.username}) }}">
                        <img src="{{ app.user.info.avatarSm }}" class="up-avatar" />
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
{% include "AppBundle:_layout:drawer.html.twig" %}
